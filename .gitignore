# Plaintext secrets — NEVER commit
.env
.env.op
.env.backup-plaintext

# Protected files (SSH keys, certs)
secrets/

# pfSense config backups contain live credentials
backups/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# macOS
.DS_Store
