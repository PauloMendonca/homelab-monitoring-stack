[mcp_servers."1password"]
command = "ssh"
args = ["truenas", "/opt/homelab/bin/1password-mcp-stdio.sh"]

# O token fica apenas no TrueNAS, lido pelo wrapper em /mnt/pool_fast/db/secrets/1password-mcp/token
