apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: notifications-infra
  namespace: argocd
spec:
  project: default
  source:
    repoURL: ssh://git@your-gitops-repo
    targetRevision: main
    path: k8s/notifications/infrastructure
  destination:
    server: https://kubernetes.default.svc
    namespace: ai-platform
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
