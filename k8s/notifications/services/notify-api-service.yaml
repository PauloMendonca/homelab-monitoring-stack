apiVersion: v1
kind: Service
metadata:
  name: notify-api
  namespace: ai-platform
  annotations:
    metallb.universe.tf/address-pool: home-network-pool
spec:
  type: LoadBalancer
  selector:
    app: notify-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
